"""
Django settings for corpus project.

Generated by 'django-admin startproject' using Django 2.1.7.

For more information on this file, see
https://docs.djangoproject.com/en/2.1/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/2.1/ref/settings/
"""

import os, sys


# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PROJECT_DIR = os.path.realpath(os.path.dirname(__file__))


CORE_DIR = os.path.join(PROJECT_DIR, "core")
APPS_DIR = os.path.join(PROJECT_DIR, "apps")
sys.path = [CORE_DIR, APPS_DIR] + sys.path


CORPUS_EXPORTS_DIR  = os.path.join(PROJECT_DIR, "exports")
CORPUS_IMPORTS_DIR  =  os.path.join(PROJECT_DIR, "imports")

TEMPL_DIR = os.path.join(PROJECT_DIR, 'templates')
JAVASCRIPT_TEMPL_DIR = os.path.join(PROJECT_DIR, 'static/js/dist')



# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/2.1/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '4c9#xm+%wb981b3b1w(p02g-c830-0375dpj-7)@^56dqyl)#k'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'rest_framework',
    'rest_framework.authtoken',
    'corsheaders',

    'django_extensions',

    'store.apps.StoreConfig',
    'task.apps.TaskConfig',
    'accounts.apps.AccountsConfig',
    'guest.apps.GuestConfig',
    'user.apps.UserConfig',
    'admin.apps.AdminConfig',
    'url_service.apps.UrlServiceConfig',
]


# Translation ref: https://youtu.be/go7JklZRtrs?t=922
from django.utils.translation import ugettext_lazy as _

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    
    # Setup LocalMiddlware to enable translation using ugettext_lazy and ugettext 
    'django.middleware.locale.LocaleMiddleware',

    'corsheaders.middleware.CorsMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'corpus.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [TEMPL_DIR, JAVASCRIPT_TEMPL_DIR],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'corpus.wsgi.application'


# Database
# https://docs.djangoproject.com/en/2.1/ref/settings/#databases

DATABASES = {
    #'default': {
    #   'ENGINE': 'django.db.backends.sqlite3',
    #   'NAME': os.path.join(PROJECT_DIR, 'dbs', 'db.sqlite3'),
    #},
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'tal_db',
        'USER': 'root',
        'PASSWORD': '',
        'HOST': '127.0.0.1',
        'PORT': '3306',
    }
}


# Password validation
# https://docs.djangoproject.com/en/2.1/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/2.1/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True

# Profive a liste of languages which your site supports
LANGUAGES = (
    ('en-us', _('English')),
    ('fr-fr',_('French(France)')),
)

# Contains the path liste where Django should look into for django.po files
# for all supported languages
LOCALE_PATHS = (
    os.path.join(PROJECT_DIR, 'locale'),
    os.path.join(PROJECT_DIR, 'locale_extra'),
)

# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/2.1/howto/static-files/



TEMP_DIR = os.path.join(PROJECT_DIR, 'temp')

STATIC_URL = '/static/'

#STATIC_ROOT = os.path.join(BASE_DIR, 'assets')

STATICFILES_DIRS = (
   os.path.join(PROJECT_DIR, "static"),
   # os.path.join(BASE_DIR, "media"),
)

MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
MEDIA_URL = '/media/'




# Cors hearder configurations
"""
CORS_ORIGIN_WHITELIST = (
    'http://localhost:8081/',
    'http://127.0.0.1:8081/',
)
"""

CORS_ORIGIN_ALLOW_ALL = True

APPEND_SLASH = False

AUTH_USER_MODEL = 'accounts.User' # changes the built-in user model to ours

# Auth
"""
AUTHENTICATION_BACKENDS = [
    'security.backends.AuthEmail'
]
"""


# Mailing configuration
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_USE_TLS = True
EMAIL_PORT = 587
EMAIL_HOST_USER = ''
EMAIL_HOST_PASSWORD = ''


APPEND_SLASH = True

#LOGOUT_REDIRECT_URL="/accounts/login/"